# Frontend environment variables
# Copy this file to .env (same directory) and adjust as needed.

# Base URL for backend API (used by src/lib/api.js)
VITE_API_BASE=http://localhost:5000/api

# Enable frontend mock data for some pages when backend is not running
VITE_USE_MOCKS=true
